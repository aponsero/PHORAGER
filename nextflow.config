// Main Phorager Pipeline Configuration
// Includes all component configurations

manifest {
    name = 'phorager'
    version = '0.5.0-beta'
    nextflowVersion = '>=24.04.0'
}

// Base pipeline configuration
includeConfig 'configs/base.config'

// Tool-specific configurations
includeConfig 'configs/conda_tools.config'
includeConfig 'configs/profiles.config'
includeConfig 'configs/tool_parameters.config'

// Bind every root a Nextflow-staged input symlink can resolve into, since
// singularity.autoMounts/runOptions don't apply to manual `singularity exec`
// calls in process scripts. Wrapped in a closure + call() because the
// strict config parser forbids top-level `def`/imperative statements
// alongside config blocks.
params.phorager_bind_str = {
    def binds = []
    binds.add(params.outdir)
    binds.add(params.database_location)
    if (params.genome) {
        def g = new File(params.genome as String).canonicalFile
        binds.add(g.isDirectory() ? g.path : g.parent)
    }
    if (params.prophage) {
        def p = new File(params.prophage as String).canonicalFile
        binds.add(p.isDirectory() ? p.path : p.parent)
    }
    if (params.extra_bind_paths) {
        binds.addAll(params.extra_bind_paths.toString().tokenize(','))
    }
    return binds.findAll { it }.unique().join(',')
}.call()

env {
    APPTAINER_BIND   = params.phorager_bind_str
    SINGULARITY_BIND = params.phorager_bind_str
}

executor {
    exitReadTimeout = '15 min'
    pollInterval = '30 sec'
    queueStatInterval = '3 min'
}

singularity.runOptions = '--cleanenv --no-home'

